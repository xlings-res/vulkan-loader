# vulkan-loader — xlings payload

Built in an xlings subos with `.agents/tools/graphics/build-in-subos.sh`
(links our glibc, leak-checked for host references).
Recipe: `xim-pkgindex/pkgs/*/vulkan-loader.lua`
